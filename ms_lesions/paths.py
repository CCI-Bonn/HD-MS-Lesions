import os

# please refer to the readme on where to get the parameters. Save them in this folder:
base_path = os.path.join(os.path.expanduser('~'), 'NeuroAI-HD/MS_Lesions')
folder_with_t1ce_parameter_files = os.path.join(base_path, 'with_t1')
folder_with_not1ce_parameter_files = os.path.join(base_path, 'without_t1')
